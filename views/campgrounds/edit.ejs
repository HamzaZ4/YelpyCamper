<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Campground</title>
</head>
<body>

  <h1>Edit Campground</h1>
<form action="/campgrounds/<%=campground.id %>?_method=PUT" method="POST">
  <div>
    <label for="title">Enter the name of the campground:</label>
    <input type="text" name="campground[title]" id="title" value="<%=campground.title %>">
  </div>


  <div>
    <label for="location">Where is this campground(city, province):</label>
    <input type="text" name="campground[location]" id="location"  value="<%=campground.location %>">
  </div>


  <button>Update</button>
</form>

<a href="/campgrounds/<%=campground._id%>">Cancel</a>

  <form action="/campgrounds/<%=campground._id%>?_method=DELETE" method="POST">
    <button>Delete Campground</button>
  </form>
</body>
</html>